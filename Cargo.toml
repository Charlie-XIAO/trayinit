[package]
name = "trayinit"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
winit = "0.30"

[target.'cfg(windows)'.dependencies.windows-sys]
version = "0.60"
features = [
  "Win32_UI_WindowsAndMessaging",
  "Win32_Foundation",
  "Win32_System_SystemServices",
  "Win32_UI_Shell",
  "Win32_Graphics_Gdi",
]
